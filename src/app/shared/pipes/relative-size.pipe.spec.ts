import { RelativeSizePipe } from './relative-size.pipe';

describe('RelativeSizePipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeSizePipe();
    expect(pipe).toBeTruthy();
  });
});
